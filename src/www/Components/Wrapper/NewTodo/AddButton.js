import Button from '@mui/material/Button';

function AddButton(props) {
    return (
        <div>
            <Button
                variant="text"
                className="btnAdd"
            >
                Add
            </Button>
        </div>
    );
}

export default AddButton;